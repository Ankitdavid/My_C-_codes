// using ternary operator

#include<iostream>
using namespace std;
int main()
{
    int n;
    cout<<"Enter the value: ";
    cin>>n;
    (n%2==0) ? cout<<"Even number" : cout<<"Odd number";
}